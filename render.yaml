services:
  - type: worker
    name: insta-group-bot
    env: python
    buildCommand: "pip install -r requirements.txt && playwright install"
    startCommand: "python instagram_group_autobot_ready.py"
